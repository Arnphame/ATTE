{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}


{% block body %}
    {% if app.user.role == 1 %}
        <style>
            .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
            .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        </style>

        <div class="main">
            {% if error is defined and error is not empty %}
                <div>  {{ error }} </div>
            {% endif %}
            {% if success is defined and success is not empty %}
                <div>  {{ success }} </div>
            {% endif %}
            <table id="services">
                <tr>
                    <th>Car</th>
                    <th>User</th>
                    <th>Service</th>
                    <th>Status</th>

                </tr>
                {% for s in carServices %}
                    <tr>
                        <td>{{ s.rulerCar }}</td>
                        <td>{{ s.rulerUser }}</td>
                        <td>{{ s.rulerService }}</td>
                        <td>{{ s.status }}</td>
                    </tr>
                {% endfor %}
            </table>
            <h1>Hello User! ✅</h1>

            This friendly message is coming from:
            <ul>
                <li>Your controller at <code>src/Controller/ProfileController.php</code></li>
                <li>Your template at <code>templates/profile/index.html.twig</code></li>
            </ul>
        </div>
    {% endif %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="main">
    {% if app.user.role == 2 %}
        {% if error is defined and error is not empty %}
            <div>  {{ error }} </div>
        {% endif %}
        {% if success is defined and success is not empty %}
            <div>  {{ success }} </div>
        {% endif %}
        <table id="services">
            <tr>
                <th>Car</th>
                <th>User</th>
                <th>Service</th>
                <th>Status</th>
                <th>Mechanic</th>
                <th>More</th>
            </tr>
            {% for s in carServices %}
                <tr>
                    <td>{{ s.rulerCar }}</td>
                    <td>{{ s.rulerUser }}</td>
                    <td>{{ s.rulerService }}</td>
                    <td>{{ s.status }}</td>
                    <td>{{ s.mechanic }}</td>
                    <td><a href = "{{ 'carservicemore/'}}">More</a></td>
                </tr>
            {% endfor %}
        </table>
        <h1>Hello ADMINISTRATOR! ✅</h1>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code>src/Controller/ProfileController.php</code></li>
            <li>Your template at <code>templates/profile/index.html.twig</code></li>
        </ul>
        </div>
    {% endif %}
{% endblock %}